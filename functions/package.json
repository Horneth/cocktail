{
  "name": "cocktail-functions",
  "private": true,
  "type": "commonjs",
  "main": "lib/functions/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "vitest run",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "test:emulator": "firebase emulators:exec --only firestore --project cocktails-c2705 \"FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 vitest run\""
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
