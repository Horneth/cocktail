{
  "functions": [
    {
      "//": "Built from ../src as well as functions/src — see functions/tsconfig.json. The prompts and schemas have exactly one home and this compiles them in; there is no copy to keep in sync.",
      "source": "functions",
      "codebase": "default",
      "runtime": "nodejs22",
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" ci",
        "npm --prefix \"$RESOURCE_DIR\" run build"
      ],
      "ignore": ["node_modules", ".git", "*.local", "**/*.test.ts"]
    }
  ],
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "//": "SPA fallback. Redundant while the app uses createHashRouter (every route lives after the #), but it is what makes a future switch to createBrowserRouter a one-line change instead of a hosting migration.",
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "//": "Default: revalidate everything. A stale service worker is exactly how the old GitHub Pages deploy pinned installed apps to a dead build, so the entry point, sw.js and the manifest must never be cached. Later matches override this for the fingerprinted files below.",
        "source": "**",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "no-cache, max-age=0"
          }
        ]
      },
      {
        "//": "Vite fingerprints everything under assets/ with a content hash, so a given URL's bytes never change.",
        "source": "/assets/**",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=31536000, immutable"
          }
        ]
      },
      {
        "//": "Icons are not fingerprinted, but they change roughly never.",
        "source": "/icons/**",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=86400"
          }
        ]
      }
    ]
  },
  "emulators": {
    "//": "Not the default 5000 — macOS AirPlay Receiver squats on that port and answers 403.",
    "hosting": {
      "port": 5055
    },
    "functions": {
      "port": 5001
    },
    "firestore": {
      "port": 8080
    },
    "ui": {
      "enabled": false
    },
    "singleProjectMode": true
  }
}
